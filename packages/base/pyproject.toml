[project]
name = "core"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.3",
    "ffmpeg-python>=0.2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"