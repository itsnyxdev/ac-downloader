[project]
name = "ac-downloader"
version = "0.1.0"
description = "Adobe Connect downloader"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "loguru>=0.7.3",
]
